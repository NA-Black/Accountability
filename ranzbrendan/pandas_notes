1. Pandas make computation in Python faster
2. Pandas dont use Python data types
3. When reading a file using pandas, it converts ints to int64.
4. int64 does not support missing values so if there are, it becomes float64.
5. can get dates if u add parse_dates()
6. converts into object if pandas does not know how to interpret some values as a number
7. Count = number of non missing values
8. df.astype({'COLUMN': 'DATA_TYPE'})
9. df.describe()
10. df.value_counts(dropna=False)
11. df.query()
12. df.select_dtypes()
13. df.memory_usage(deep=True)
14. df.sum()
15. np.iinfo(np.int8)
16. np.finfo(np.float16)
17. CARDINALITY = Describes amount of unique values
18. EXPLORATORY DATA ANALYSIS
19. df.groupby()
20. df.nunique()
21. df.unique()
22. df.<column>.contains() -> boolean?
23. df.<column>.extract(<regex>)
24. pd.to_datetime(df.<column>).dt.tz_localize('America/New_York')
25. Chaining is more efficient, because operations return new objects.
26. df.groupby() - numeric_only values
27. df.mean()
28. df.agg()
29. df.unstack()
30. df.plot() - requires matplotlib
31. df.legend()
32. df.gt() -> boolean - greater than
33. df.pipe()
